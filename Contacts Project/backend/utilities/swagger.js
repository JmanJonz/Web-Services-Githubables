// imports
    import swaggerJSDoc from "swagger-jsdoc";
    import swaggerJSDoc from "swagger-jsdoc";

const swaggerOptions = {
    swaggerDefinition: {
        info: {
            title: "Contacts Project",
            version: "100000"
        }
    }
}